minimize:
    x1 + x2 + x3 + x4 + x5 + x6
subject to:
    x1 + x2 >= 1
    x2 + x3 >= 1
    x3 + x4 >= 1
    x1 + x5 >= 1
    x2 + x5 >= 1
    x3 + x6 >= 1
notes:
Compute the minimum vertex cover of the following graph:

1 -- 2 -- 3 -- 4
  \  |    |
   \ |    |
     5 -- 6

Possible solutions:
{1,3,5}, {2,3,5}
